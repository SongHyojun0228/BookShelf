***[ Books 테이블에 넣기 전에 이미 있는 책인지 확인하는 로직 ] ***
- Addbookmoal.tsx에서 책을 담기 전에 Books Table 에 존재하는 책인지 검사
const { data: existingBook }
    = await supabase.from('Books')
    .select('book_id')
    .eq('title', book.title)
    .eq('author', book.author)
    .single();

    let insertedBookId;

    if (existingBook) {
        insertedBookId = existingBook.book_id
        console.log('이미 Books Table에 존재하는 책 : ', book.title)
    } else { ... }