***[ 내 서재에 책 리스트업 ]***

**MyLibrary Table + Books Table + Users Table**
- FK 
- MyLibrary에 있는 book_id로 검색
- user_id도 

**[ 1. JOIN ]**
.select(`
    *,
    Books (
        *
    )
`)
- *첫 번째 *: 현재 기준이 되는 테이블(MyLibrary)의 모든 칼럼을 가져오라는 뜻*
- *Books ( * ): 여기가 바로 마법의 구문*
    - MyLibrary 테이블에는 book_id라는 칼럼 존재
    - 그 숫자가 가리키는 Books 테이블을 통째로 엮어서(Join) 가져오되, Books 테이블에 있는 모든 칼럼(*)도 다 가져오라는 뜻
{
  "mylibrary_id": 1,
  "status": "읽는 중",
  "book_id": 5,
  "Books": {         <-- 연결된 책 껍데기 정보가 이 뭉치에 다 들어옴!
    "title": "해리포터",
    "author": "조앤 K 롤링",
    "img": "http..."
  }
}
- *.eq / .eq('user_id', UUID)* 
